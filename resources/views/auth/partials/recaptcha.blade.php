{{-- @section('recaptcha')
         <div class="row">
            <div class="input-field col s8">
			    {!! NoCaptcha::display() !!}
			    @if ($errors->has('g-recaptcha-response'))
				    <span class="new red badge left">
				        <strong>{{ $errors->first('g-recaptcha-response') }}</strong>
				    </span>
				@endif
			</div>
		</div>
@endsection
 --}}
